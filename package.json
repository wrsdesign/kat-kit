{
  "scripts": {
    "setup": "cd www && composer install",
    "dev": "touch src/.lock && vite",
    "build": "rm -f src/.lock && vite build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.4",
    "tailwindcss": "^3.0.1",
    "vite": "^2.7.1",
    "vite-plugin-live-reload": "^2.1.0"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  }
}
