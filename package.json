{
  "scripts": {
    "setup": "npm install && composer install && composer update",
    "dev": "npm run serve & touch src/.lock && vite",
    "serve": "php -S localhost:8080 -t ./ kirby/router.php",
    "build": "rm -f src/.lock && vite build"
  },
  "devDependencies": {
    "@tailwindcss/aspect-ratio": "^0.4.0",
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.1.6",
    "vite": "^2.9.14",
    "vite-plugin-live-reload": "^2.1.0"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  },
  "dependencies": {
    "@alpinejs/intersect": "^3.10.2",
    "alpinejs": "^3.10.2",
    "loading-attribute-polyfill": "^2.1.0",
    "prevent-scrolling": "^1.3.10",
    "vanilla-lazyload": "^17.8.2"
  }
}
