{
  "scripts": {
    "setup": "npm install && cd www && composer install",
    "dev": "npm run kirby & touch src/.lock && vite",
    "kirby": "php -S localhost:8080 -t www/public www/kirby/router.php",
    "build": "rm -f src/.lock && vite build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.15",
    "vite": "^2.7.13",
    "vite-plugin-live-reload": "^2.1.0"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  },
  "dependencies": {
    "alpinejs": "^3.9.0"
  }
}
